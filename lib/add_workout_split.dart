import 'package:flutter/material.dart';

class AddWorkoutSplit extends StatelessWidget {
  const AddWorkoutSplit({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Add Workout Split")),
      body: Center(child: Text("Add Workout Split Form")),
    );
  }
}
